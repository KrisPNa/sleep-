<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="24dp"> <!-- Убрали общий padding -->

    <!-- Раздел коллекций с собственным padding -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="16dp"
        android:paddingBottom="12dp">

        <TextView
            android:id="@+id/collectionsTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Мои коллекции"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="@color/text_dark" />

        <TextView
            android:id="@+id/collectionsCount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0"
            android:textSize="16sp"
            android:textColor="@color/white"
            android:background="@drawable/counter_background"
            android:paddingHorizontal="12dp"
            android:paddingVertical="4dp" />

        <ImageButton
            android:id="@+id/collectionsSortButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_baseline_more_vert_24"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:padding="8dp"
            android:layout_marginStart="8dp"
            android:contentDescription="Меню сортировки" />
    </LinearLayout>

    <!-- Если коллекций нет -->
    <TextView
        android:id="@+id/noCollectionsText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Коллекций пока нет. Создайте первую!"
        android:textSize="16sp"
        android:textColor="@color/text_gray"
        android:gravity="center"
        android:padding="32dp"
        android:visibility="gone" />

    <!-- Список коллекций - без отступов -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/collectionsRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingBottom="24dp" />

</LinearLayout>