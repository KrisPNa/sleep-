<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:padding="16dp"
    android:paddingBottom="80dp"
    tools:context=".ui.screens.AddSeriesScreen">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

        <ImageButton
            android:id="@+id/backButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:paddingTop="30dp"
            android:src="@drawable/ic_baseline_arrow_back_24"
            android:background="@android:color/transparent"
            android:contentDescription="Кнопка назад" />

        <!-- Название сериала -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Название сериала:"
            android:textSize="16sp"
            android:textStyle="bold"
            android:paddingTop="20dp"
            android:textColor="@color/text_dark"
            android:layout_marginBottom="8dp" />
    </LinearLayout>
        <EditText
            android:id="@+id/titleEditText"
            style="@style/Widget.App.EditText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/text_dark"
            android:hint="Введите название"
            android:inputType="text"
            android:imeOptions="actionNext"
            android:minHeight="48dp"
            android:backgroundTint="@color/primary_blue"
            android:layout_marginBottom="16dp" />

        <!-- Заметки -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Заметки (необязательно):"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@color/text_dark"
            android:layout_marginBottom="8dp" />

        <EditText
            android:id="@+id/notesEditText"
            style="@style/Widget.App.EditText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/text_dark"
            android:hint="Дополнительная информация"
            android:inputType="textMultiLine"
            android:imeOptions="actionDone"
            android:minLines="3"
            android:minHeight="120dp"
            android:backgroundTint="@color/primary_blue"
            android:layout_marginBottom="16dp" />

        <!-- Изображение -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Изображение (необязательно):"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@color/text_dark"
            android:layout_marginBottom="8dp" />

        <Button
            android:id="@+id/selectImageButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Выбрать изображение"
            android:minHeight="48dp"
            android:layout_marginBottom="16dp" />

        <ImageView
            android:id="@+id/seriesImageView"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:layout_gravity="center"
            android:scaleType="centerCrop"
            android:background="@android:color/darker_gray"
            android:contentDescription="Изображение сериала"
            android:layout_marginBottom="16dp" />

        <!-- Выбор коллекций -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Выберите коллекции:"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@color/text_dark"
            android:layout_marginBottom="8dp" />

        <LinearLayout
            android:id="@+id/collectionsLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="8dp"
            android:background="@color/background_gray"
            android:layout_marginBottom="24dp" />

        <!-- Кнопка сохранения -->
        <Button
            android:id="@+id/saveButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Добавить сериал"
            android:textSize="16sp"
            android:minHeight="48dp"
            android:layout_marginBottom="24dp"/>

    </LinearLayout>

</ScrollView>